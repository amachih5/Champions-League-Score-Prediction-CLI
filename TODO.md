# CLPredictor TODO / 進捗

> 分析日: 2026/09/21 ｜ 提出(GitHub+スライド): **9/25(金)** ｜ 発表: 9/28〜29

## 📊 進捗サマリー（ざっくり 40%）

| 部分 | 状態 |
| --- | --- |
| プロジェクト構成（package分け） | ✅ 完了 |
| Match（データの箱・getter/setter） | ✅ 完了 |
| メニュー表示＋ループ（Main / MenuConstant） | ✅ 完了 |
| Scanner 1本化 | ✅ 完了 |
| 1. 登録（Create） | 🟡 ほぼ完成（入力ズレの小バグあり） |
| 2. 一覧（Read） | ⬜️ 中身これから |
| 3. 更新（Update） | ⬜️ 中身これから |
| 4. 削除（Delete） | ⬜️ 中身これから |
| 5. 判定（Judge） | ⬜️ Judgeクラス空っぽ |
| 勝ち点の自動計算 | ⬜️ まだ |
| 例外処理・入力チェック | ⬜️ まだ |

---

## 🔴 コード（機能を動かす）※上から順がおすすめ

- [ ] **登録の入力ズレを直す**
  - `registerMatchDetails`：id の `nextInt()`（19行目）の直後に `scan.nextLine();` を足す
  - 27行目の余分な `scan.nextLine();` を消す
  - → これで homeTeam が空になるバグが直る
- [ ] **2. 一覧表示（Read）を実装** … `for (Match m : matches)` で全件表示。空なら「登録なし」
- [ ] **勝ち点の計算を用意** … 予想/実際のスコアから 勝3・分1・敗0 を出す（Read・Judgeで使う）
- [ ] **3. 更新（Update）を実装** … id を指定 → 実際スコアを入れる／予想を直す
- [ ] **4. 削除（Delete）を実装** … id を指定 → 1件消す（危険なので確認メッセージ）
- [ ] **5. 判定（Judge）を実装** … 的中数・的中率・勝ち点のズレを出す
- [ ] **例外処理・入力チェック** … 数字以外を入れても落ちないように

## 🟡 設計の決めごと（先に決めると実装が楽）

- [ ] **実際スコアは「登録時」？「更新時」？** … 設計は"更新時"（Integerでnull可にした理由）。今は登録時に聞いてる
- [ ] **id は自動採番にする？** … 今は手入力。`matches.size() + 1` で自動にできる
- [ ] **InputUtil を使う？** … 今は空。範囲チェックは showMenu にある。使わないなら設計から外す

## 🟢 提出物（9/25まで）

- [ ] README の「使い方（How to run）」を埋める
- [ ] GitHub に push（`.gitignore`でJDK除外済み）
- [ ] 発表スライド作成（概要・ターゲット・機能・工夫点・苦労点・今後・デモ）

---

## メモ
- データはメモリ保持（アプリ終了で消える）。CSV保存は"推奨機能"なので、余裕があれば。
- 今書いたコードは全部自分の手 → 口頭確認で説明できる状態。この調子を維持。
